"""CLI entrypoints (stubs) for single-command and REPL modes.

This module will contain the command-line interface for the web search agent,
allowing users to perform searches directly from the terminal.
"""
