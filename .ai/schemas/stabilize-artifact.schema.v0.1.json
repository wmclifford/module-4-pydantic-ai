{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/wmclifford/ai-coding-support/schemas/stabilize-artifact.schema.v0.1.json",
  "title": "Stabilize Artifact v0.1",
  "description": "Schema for .ai/task-artifacts/<TASK-ID>/stabilize.yaml generated by the stabilize pass.",
  "type": "object",
  "required": [
    "task_id",
    "author",
    "created_at",
    "spec_reference",
    "applied_commits"
  ],
  "properties": {
    "task_id": {
      "description": "Task ID, e.g., \"TASK-001\". Must match the task ID in the prompt header.",
      "type": "string"
    },
    "author": {
      "description": "Name of the operator who initiated the task",
      "type": "string"
    },
    "created_at": {
      "description": "Timestamp when the stabilize artifact was created",
      "type": "string",
      "format": "date-time"
    },
    "spec_reference": {
      "description": "Reference to the spec artifact used for this stabilization pass",
      "type": "string"
    },
    "applied_commits": {
      "description": "List of applied commits",
      "type": "array",
      "items": {
        "description": "SHA of the commit that applied the change",
        "type": "string"
      }
    },
    "testSummary": {
      "description": "Test summary",
      "anyOf": [
        {
          "description": "No tests were run",
          "type": "null"
        },
        {
          "description": "Test summary as a string",
          "type": "string"
        },
        {
          "description": "Test summary as an array of strings",
          "type": "array",
          "items": {
            "description": "Test summary line",
            "type": "string"
          }
        },
        {
          "description": "Test summary as an object",
          "type": "object"
        }
      ]
    },
    "lintSummary": {
      "description": "Lint summary",
      "type": "object"
    },
    "unresolved_questions": {
      "description": "List of questions that were not answered during the stabilize pass",
      "type": "array",
      "items": {
        "description": "Question that was not answered during the stabilize pass",
        "type": "string"
      }
    },
    "version": {
      "description": "Version of the stabilization pass",
      "type": "string"
    }
  },
  "additionalProperties": false
}

