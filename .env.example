# LLM Configuration
# The LLM provider to use (e.g., 'openai', 'anthropic', 'ollama')
LLM_PROVIDER=openai

# Optional: Base URL for the LLM API (useful for custom endpoints or local deployments)
# Leave empty or unset to use the provider's default endpoint
LLM_BASE_URL=

# API key for authentication with the LLM provider
# For OpenAI: https://platform.openai.com/api-keys
# For Anthropic: https://console.anthropic.com/
# For Ollama: typically not required for local deployments
LLM_API_KEY=your-api-key-here

# The specific model choice to use
# Examples: 'gpt-4', 'gpt-3.5-turbo', 'claude-3-opus', 'claude-3-sonnet'
LLM_CHOICE=gpt-4

# Search Backend Configuration
# At least one of the following must be configured

# Brave Search API Key
# Get your API key from: https://api.search.brave.com/
# Leave empty or unset to disable Brave Search
BRAVE_API_KEY=

# SearXNG Base URL
# The base URL of your SearXNG instance (e.g., https://searxng.example.com)
# Leave empty or unset to disable SearXNG
SEARXNG_BASE_URL=

